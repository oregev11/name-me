# Python
__pycache__/
*.pyc
.venv/
*.egg-info/
.pytest_cache/
.ruff_cache/
backend/.python-version

# Note: backend/src/nameme/artifacts/* is intentionally NOT ignored -- the
# precomputed corpus + embedder + PCA artifacts are committed on purpose.

# Pipeline status monitor (regenerated on every script run, not committed)
backend/scripts/.pipeline_status.json
backend/scripts/pipeline_status.html

# run.sh output
.run-logs/

# Jupyter (notebooks/ml_sanity_check.ipynb itself IS committed, with outputs)
.ipynb_checkpoints/

# Node
node_modules/
dist/
frontend/.vite/

# Env files (commit the .env.example templates instead)
.env
*.env.local

# Editors / OS
.vscode/
.idea/
.DS_Store
